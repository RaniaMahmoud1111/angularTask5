export const environment = {
  production: false,
  /** Base URL from your MockAPI project (https://mockapi.io) — resources: `categories`, `courses` */
  apiBaseUrl: 'https://YOUR_MOCKAPI_ID.mockapi.io',
};
